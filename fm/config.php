<?php
/**
 * 文件管理器配置文件
 * 
 * 此文件会覆盖 index.php 中的默认配置
 * 请修改以下变量以满足您的需求
 */

// ============================================================================
// 基本设置
// ============================================================================

// 应用标题
define('APP_TITLE', '文件管理器');

// ============================================================================
// 认证设置
// ============================================================================

// 用户名
$auth_user = 'admin';

// 密码(使用 bcrypt 哈希)
// 生成方法: php -r "echo password_hash('你的密码', PASSWORD_BCRYPT, ['cost' => 10]);"
$auth_pass = '$2y$05$ZsvDrNti8d7.qJ4sI67cWO1LoUK1hCr.uPMUwWNEvTTY/XFwGW9zy';  // 密码: admin

// ============================================================================
// 路径设置
// ============================================================================

// 文件操作根目录(限制文件管理器只能在此目录下操作)
// 示例:
//   $root_path = $_SERVER['DOCUMENT_ROOT'];           // Web根目录
//   $root_path = __DIR__ . '/files';                  // 当前目录下的 files 文件夹
//   $root_path = 'D:/uploads';                        // 指定绝对路径
$root_path = $_SERVER['DOCUMENT_ROOT'];

// 分享链接基础URL(留空则自动检测)
// 仅在反向代理等特殊场景需要手动指定
// 示例: $base_url = 'https://github.com/';
$base_url = '';

// ============================================================================
// 上传设置
// ============================================================================

// 最大上传文件大小(字节)
// 示例:
//   100MB = 100 * 1024 * 1024 = 104857600
//   1GB = 1024 * 1024 * 1024 = 1073741824
//   5GB = 5 * 1024 * 1024 * 1024 = 5368709120
$max_upload_size = 5000000000;  // 5GB

// 禁止上传的文件扩展名
$forbidden_extensions = [
    'php', 'php3', 'php4', 'php5', 'phtml', 'phar',
    'asp', 'aspx', 'jsp', 'cgi', 'pl', 'py',
    'sh', 'bash', 'exe', 'bat', 'cmd', 'com',
    'vbs', 'vbe', 'js', 'jse', 'wsf', 'wsh',
    'htaccess', 'ini', 'shtml', 'stm', 'shtm'
];

// ============================================================================
// 登录安全设置
// ============================================================================

// 最大失败尝试次数
$login_max_attempts = 5;

// 锁定时间(秒)
// 示例:
//   15分钟 = 900
//   30分钟 = 1800
//   1小时 = 3600
$login_lockout_time = 900;  // 15分钟
